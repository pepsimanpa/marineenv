<Window x:Class="MarineEnvironment.Viewer.NetCdfInspectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetCDF Inspector" Width="1120" Height="780" MinWidth="900" MinHeight="620"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="190"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#D0D7DE" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="NetCDF file" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                <TextBox x:Name="FilePathTextBox" Grid.Column="1" MinWidth="420" VerticalContentAlignment="Center"/>
                <Button Grid.Column="2" Margin="8,0,0,0" Padding="12,6" Click="BrowseFile_Click">Browse...</Button>
                <Button Grid.Column="3" Margin="8,0,0,0" Padding="12,6" Click="OpenFile_Click" FontWeight="SemiBold">Open</Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="430"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#D0D7DE" BorderThickness="1" CornerRadius="6" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="150"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Dimensions" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <DataGrid x:Name="DimensionsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Length" Binding="{Binding LengthText}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Grid.Row="2" Text="Variables" FontSize="15" FontWeight="SemiBold" Margin="0,12,0,6"/>
                    <DataGrid x:Name="VariablesGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" SelectionMode="Single" SelectionChanged="VariablesGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="95"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="70"/>
                            <DataGridTextColumn Header="Dimensions" Binding="{Binding DimensionText}" Width="*"/>
                            <DataGridTextColumn Header="Fill" Binding="{Binding FillValueText}" Width="90"/>
                            <DataGridTextColumn Header="First" Binding="{Binding FirstValueText}" Width="90"/>
                            <DataGridTextColumn Header="Last" Binding="{Binding LastValueText}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent"/>

            <Border Grid.Column="2" BorderBrush="#D0D7DE" BorderThickness="1" CornerRadius="6" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="190"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Raw sample" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <DataGrid x:Name="IndicesGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Dimension" Binding="{Binding DimensionName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Length" Binding="{Binding LengthText}" Width="90" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Index" Binding="{Binding Index, UpdateSourceTrigger=PropertyChanged}" Width="90"/>
                            <DataGridTextColumn Header="Coordinate" Binding="{Binding CoordinateText}" Width="150" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="2" Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Padding="12,6" Click="ReadValue_Click" FontWeight="SemiBold">Read Value</Button>
                        <Button Grid.Column="1" Margin="8,0,0,0" Padding="12,6" Click="ReadPeerRecord_Click">Read same-index 1D record</Button>
                        <TextBlock x:Name="SelectedValueText" Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Center" Text="Value: -" TextWrapping="Wrap"/>
                    </Grid>

                    <TextBox x:Name="PeerRecordTextBox" Grid.Row="3" Margin="0,8,0,0" MinHeight="54" MaxHeight="88" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="#F6F8FA" BorderBrush="#D8DEE4"/>

                    <Border Grid.Row="4" Margin="0,10,0,8" Padding="8" BorderBrush="#D8DEE4" BorderThickness="1" CornerRadius="4" Background="#F8FAFC">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Series dimension" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <ComboBox x:Name="SeriesDimensionComboBox" Grid.Column="1" DisplayMemberPath="DimensionName"/>
                            <TextBlock Grid.Column="2" Text="Start" VerticalAlignment="Center" Margin="12,0,6,0"/>
                            <TextBox x:Name="SeriesStartTextBox" Grid.Column="3" Text="0" VerticalContentAlignment="Center"/>
                            <TextBlock Grid.Column="4" Text="Count" VerticalAlignment="Center" Margin="12,0,6,0"/>
                            <TextBox x:Name="SeriesCountTextBox" Grid.Column="5" Text="100" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="6" Margin="12,0,0,0" Padding="12,5" Click="ReadSeries_Click" FontWeight="SemiBold">Read Series</Button>
                            <TextBlock Grid.Column="7" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#57606A" Text="Maximum 10,000 values per read"/>
                        </Grid>
                    </Border>

                    <DataGrid x:Name="SeriesGrid" Grid.Row="5" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Index" Binding="{Binding Index}" Width="90"/>
                            <DataGridTextColumn Header="Coordinate" Binding="{Binding CoordinateText}" Width="180"/>
                            <DataGridTextColumn Header="Raw value" Binding="{Binding ValueText}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Margin="0,10,0,0" BorderBrush="#D0D7DE" BorderThickness="1" CornerRadius="6" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Export output" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Margin="12,0,0,0" Padding="12,5" Click="ShowTextExport_Click">Show Text</Button>
                    <Button Grid.Column="2" Margin="8,0,0,0" Padding="12,5" Click="ShowJsonExport_Click">Show JSON</Button>
                    <Button Grid.Column="3" HorizontalAlignment="Right" Padding="14,5" Click="CopyExport_Click" FontWeight="SemiBold">Copy</Button>
                </Grid>
                <TextBox x:Name="ExportOutputTextBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap" FontFamily="Consolas" FontSize="12"
                         Background="#F6F8FA" BorderBrush="#D8DEE4"/>
            </Grid>
        </Border>

        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem><TextBlock x:Name="InspectorStatusText" Text="Open a .nc file to inspect its raw structure."/></StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
